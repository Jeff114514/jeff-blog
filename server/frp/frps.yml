bindAddr: 0.0.0.0
bindPort: ${FRP_BIND_PORT:-7000}

auth:
  method: token
  token: "Wsygb!23"

log:
  to: "/var/log/frp/frps.log"
  level: info
  maxDays: 3
  disablePrintColor: false

webServer:
  addr: 0.0.0.0
  port: ${FRP_DASHBOARD_PORT:-7500}
  user: "${FRP_DASHBOARD_USER:-admin}"
  password: "${FRP_DASHBOARD_PASSWORD:-admin123}"

transport:
  tcpMux: true

vhostHTTPPort: 8080
vhostHTTPSPort: 8443

subdomainHost: "xn--viqt41cw7bz9i.icu"

allowPorts:
  - start: 2000
    end: 3000
  - start: 5000
    end: 6000
  - start: 8000
    end: 9000
  - single: 11434
